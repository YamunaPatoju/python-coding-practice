# Rotate Doubly Linked List

## Problem

Given the head of a doubly linked list, rotate the linked list counter-clockwise by `k` nodes.

The rotation moves the first `k` nodes to the end of the linked list.

## Example

### Input

```text
1 <-> 2 <-> 3 <-> 4 <-> 5 <-> 6
k = 2
