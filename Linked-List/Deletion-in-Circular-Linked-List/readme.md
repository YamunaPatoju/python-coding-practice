# Deletion in Circular Linked List
## Problem

Given the **head** of a circular linked list and an integer `key`, delete the **first occurrence** of the node containing `key`, if it exists.

Return the head of the modified circular linked list.

## Examples

### Example 1

**Input:**

```text
1 -> 7 -> 8 -> 10 -> back to 1
key = 8
